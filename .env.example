# Copy to .env and adjust. Both runtimes (Python argus/, Node dashboard/) and all
# scheduled jobs resolve config from here. Secrets NEVER go in launchd plists.

# Canonical SQLite database (absolute path)
ARGUS_DB=/Users/you/Market_Analyse/argus.db

# Directory containing bridge_latest.csv + dated bridge CSVs
BRIDGE_DIR=/Users/you/Market_Analyse/reports

# Account-trust backtest CSV (generated by the daily pipeline's backtest step; may not exist until the first scheduled run)
ACCOUNTS_CSV=/Users/you/Market_Review/reports/account_backtest.csv
