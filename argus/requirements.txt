# --- Core ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# --- Market data ---
yfinance>=0.2.40
pandas>=2.2
# yfinance parses the earnings-dates table with it; without lxml
# Ticker.get_earnings_dates raises and earnings lose their BMO/AMC clock.
lxml>=5.0
numpy>=1.26
scipy>=1.11
# Indicators are implemented in pure pandas/numpy — no external TA library.

# --- Broker (IBKR) ---
ib_insync>=0.9.86

# --- Storage ---
SQLAlchemy>=2.0
aiosqlite>=0.20

# --- Alerts ---
python-telegram-bot>=21.0
httpx>=0.27

# --- AI / chat (optional, only if ANTHROPIC_API_KEY is set) ---
anthropic>=0.39

# --- MCP server ---
fastmcp>=0.4

# --- Util / dev ---
rich>=13.7
typer>=0.12

# --- Offline analysis (tools/weight_opt, tools/analysis) ---
# Not imported by the API or MCP server, but the scheduled weight-revalidation
# job runs on this venv and dies on whichever of these is absent. The 2026-07-21
# firing exited 127 on a dead interpreter; the 2026-08-03 re-run then hit three
# separate ImportErrors in a row. Pinned here so the next one fails on data, not
# on the environment.
scikit-learn>=1.4
matplotlib>=3.8
pyarrow>=15.0

# --- Testing ---
pytest-asyncio>=0.23
